    https://codepen.io/FinnTheDol/pen/VwaQLxY?editors=1000
    1. What is Semantic HTML?
HTML written in a way that provides meaning to people reading the code. 
    2. What is HTML used for?
html is used to mark up webpages with information. 
    3. What is an attribute and where do we put it?
after the tag, before the >. 
    4. What is the h1 tag used for? How many times should I use it on a page?
the h1 tag is meant to identify the main header of a website, you should use it only once.
    5. Name two tags that have required attributes
<img> <a>
    6. What do we put in the head of our HTML document? 
metadata.
    7. What is an id?
an id is an additional way to differentiate between two items that may use the same tag. 
    8. What elements can I add an id to? 
all elements except <base> , <head> , <html> , <meta> , <script> , <style> , and <title>
    9. How many times can I use the same id on a page? 
once
    10. What is a class? 
a class is an attribute that can be given to multiple items to select all of them at once.
    11. What elements can I add a class to? 
any HTML element.
    12. How many times can I use the same class on a page? 
as many times as you want.
    13. How do I get my link to open in a new tab?
by adding target="_blank" to the end of the link in your anchor tag.
    14. What is the alt attribute in the image tag used for? 
it gives alternate text in the event that the image for some reason cannot be viewed
    15. How do I reference an id?
by using a # followed by the name of the element
    16. What is the difference between a section and a div
the section tag is meant to group related items, while the div tag contains no meaning.
    17. What is CSS used for? 
CSS is used to style the HTML that is written and make it look prettier.
    18. How to we select an element? Example - every h2 on the page
#elementName
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
a class can be given to multiple elements, where as an ID should only be given to one. an example would be you would use a class to select multiple different elements that you wanted to share attributes, where as an ID would be used to select a singular element and only change it.
    20. How do we select classes in CSS?
.className
    21. How do we select a p element with a single class of “human””?
.human{}
    22. What is a parent child selector? When would this be useful?
it selects all elements that are a direct child of the specified element. could be useful in selecting the objects within an unordered list. 
    23. How do you select all links within a div with the class of sidebar?
div.sidebar{}
    24. What is a pseudo selector?
a way to select things under certain conditions.
    25. What do we use the change the spacing between lines?
editing the line-height value.
    26. What do we use to change the spacing between letters?
editing the letter-spacing value.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
text-transform:uppercase, text-transform:lowercase, text-transform:capitalize
    28. How do I add a 1px border around my div that is dotted and black?
div{
  border-width: 1px;
  border: dotted;
  border-color: black;
}
    29. How do I select everything on the page? 
*{}
    30. How do I write a comment in CSS?
start with /* and end with */
    31. How do I find out what file I am in, when I am using the command line?
pwd 
    32. Using the command line - how do I see a list of files/folders in my current folder?
dir
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
rm. with some systems, you wont be prompted before deleting the file, and could lose alot of work.
    34. Why should I use version control? 
it keeps a history of all changes made.
    35. How often should I commit to github?
each time you make a change focused on a single problem. 
    36. What is the command we would use to push our repo up to github? 
the git push command
    37. Walk me through Lambda's git flow. 
